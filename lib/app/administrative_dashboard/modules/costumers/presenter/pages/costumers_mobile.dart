import '../../../../../../core/design_system/design_system.dart';

class CostumersMobile extends StatelessWidget {
  const CostumersMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
