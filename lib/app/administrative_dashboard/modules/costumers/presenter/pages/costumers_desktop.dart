import '../../../../../../core/design_system/design_system.dart';

class CostumersDesktop extends StatelessWidget {
  const CostumersDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.amber,);
  }
}