import '../design_system.dart';
import 'colors_app.dart';

var shadowTres = BoxShadow(
  color: ColorsAppLight.shadow.withOpacity(0.06),
  spreadRadius: 0,
  blurRadius: 12,
  offset: const Offset(0, 4),
);
